
{%
set classes = [
  'entity-' ~ entity['#entity_type']|clean_class,
  'bundle-' ~ entity['#entity'].type.target_id|clean_class
]
%}

<div {{ attributes.addClass(classes) }}>
    {{ entity }}
</div>
